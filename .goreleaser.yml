builds:
  - main: cmd/nem/main.go
    binary: nem
    goos:
      - windows
      - darwin
      - linux
    goarch:
      - amd64

archive:
  format: tar.gz
  replacements:
    amd64: 64-bit
    darwin: macOS

snapshot:
  name_template: SNAPSHOT-{{.Commit}}